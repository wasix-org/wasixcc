name: Test GitHub Action

on:
  pull_request:
    paths:
      - 'action.yml'
      - '.github/workflows/test-action.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-action:
    name: Test Action on ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-24.04
            runner: ubuntu-24.04
          - name: ubuntu-24.04-arm
            runner: ubuntu-24.04-arm
          - name: macos-15
            runner: macos-15
          - name: macos-15-intel
            runner: macos-15-intel
          - name: alpine-3.23-x86_64
            runner: ubuntu-24.04
            container: alpine:3.23
          - name: alpine-3.23-aarch64
            runner: ubuntu-24.04-arm
            container: alpine:3.23
    container: ${{ matrix.container }}
    
    steps:
      - name: Install git (Alpine only)
        if: contains(matrix.name, 'alpine')
        run: apk add --no-cache git
      
      - name: Clone repository
        run: |
          git clone --depth=1 --branch=${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}} https://github.com/${GITHUB_REPOSITORY}.git .
      
      - name: Setup wasixcc with all components
        uses: ./
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Verify wasixcc installation
        run: |
          echo "Testing wasixcc..."
          wasixcc --version
          
          echo "Testing wasix++..."
          wasix++ --version || echo "wasix++ not found"
          
          echo "Checking sysroot..."
          wasixcc --print-sysroot
      
      - name: Create and compile C program
        run: |
          cat > hello.c << 'EOF'
          #include <stdio.h>
          int main() {
              printf("Hello from WASIX!\n");
              return 0;
          }
          EOF
          wasixcc hello.c -o hello.wasm
          ls -lh hello.wasm
      
      - name: Create and compile C++ program
        run: |
          cat > hello.cpp << 'EOF'
          #include <iostream>
          int main() {
              std::cout << "Hello from WASIX C++!" << std::endl;
              return 0;
          }
          EOF
          wasix++ hello.cpp -o hello_cpp.wasm
          ls -lh hello_cpp.wasm

  test-action-minimal:
    name: Test Minimal Action on ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-24.04
            runner: ubuntu-24.04
          - name: ubuntu-24.04-arm
            runner: ubuntu-24.04-arm
          - name: macos-15
            runner: macos-15
          - name: macos-15-intel
            runner: macos-15-intel
          - name: alpine-3.23-x86_64
            runner: ubuntu-24.04
            container: alpine:3.23
          - name: alpine-3.23-aarch64
            runner: ubuntu-24.04-arm
            container: alpine:3.23
    container: ${{ matrix.container }}
    
    steps:
      - name: Install git (Alpine only)
        if: contains(matrix.name, 'alpine')
        run: apk add --no-cache git
      
      - name: Clone repository
        run: |
          git clone --depth=1 --branch=${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}} https://github.com/${GITHUB_REPOSITORY}.git .
      
      - name: Setup wasixcc (minimal - no toolchain)
        uses: ./
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          install-sysroot: 'false'
          install-llvm: 'false'
          install-binaryen: 'false'
      
      - name: Verify wasixcc installation
        run: |
          echo "Testing wasixcc..."
          wasixcc --version

